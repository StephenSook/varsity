# Dependencies
node_modules/
.pnp/
.pnp.js

# Builds / output
dist/
build/
out/
.next/
.turbo/
*.tsbuildinfo
coverage/
.vite/
dev-dist/

# Playwright (e2e / a11y)
test-results/
playwright-report/
blob-report/
.playwright/

# Python
__pycache__/
*.py[cod]
.venv/
.venv-ingest/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
*.egg-info/

# Env / secrets (NEVER commit real values)
.env
.env.local
.env.*.local
*.pem
*.key
service-account*.json
infra/secrets/

# Model weights / large artifacts (host externally or via LFS)
*.onnx
*.gguf
*.safetensors
models/
*.pdf

# RAG raw intermediates (the small built index in services/app/rag/index/ IS
# committed so the app/Docker/CI ship the real corpus without re-running Docling).
services/app/rag/corpus/
services/data/

# OS / IDE
.DS_Store
.AppleDouble
._*
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*

# Keep these tracked
!.env.example

# Vercel CLI link
.vercel/
