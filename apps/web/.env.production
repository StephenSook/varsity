# Tracked on purpose: VITE_ vars are inlined into the public client bundle, so this is not a
# secret (the backend URL already ships in the bundle and the README). No real env file is tracked.
VITE_BACKEND_URL=https://varsity-api.onrender.com
