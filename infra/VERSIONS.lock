# Pinned versions observed at build start (2026-06-01). Re-verify before relying on any.

context-forge:    1.0.2     # GitHub release latest stable 2026-05-26 (Admin UI Rewrite).
                            # IMPORTANT: the ghcr DOCKER image versioned tags lag (max 0.5.0);
                            # there is NO 1.0.2 image tag. GA 1.0.x ships via the `latest`
                            # image and PyPI `mcp-contextforge-gateway==1.0.2`. Compose uses
                            # `latest` locally; pin via a pip Dockerfile for Render deploy.
mcp-python-sdk:   1.27.2
fastmcp:          3.3.1     # stable. The "70% of MCP servers" claim is maintainer self-reported.
granite-reasoning: ibm/granite-4-h-small      # verified in watsonx catalog 2026-06-01; fallback ibm/granite-3-8b-instruct
granite-guardian:  ibm/granite-guardian-3-8b  # only Guardian on watsonx (not v4); apply BYOC criteria via prompt
a2a-method:       message/send   # not legacy task/send; Agent Card at /.well-known/agent-card.json

# Front end (re-verify via context7 at build start; the §2 table is a snapshot)
node:             22
vite:             6.x
react:            19.x
tailwindcss:      4.x
three:            r183       # @react-three/fiber 9 | drei 10 (skip v11 alpha)
transformers-js:  4.x        # npm package name: @huggingface/transformers
anime-js:         4.x        # ESM only: import { animate, createTimeline } from 'animejs'
