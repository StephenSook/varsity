# Build-time / local only (NOT installed in CI). Used by the data-pull and RAG-ingest scripts.
# The runtime needs faiss-cpu + numpy too (see requirements.txt); they live there now.
statsbombpy>=1.14
# IFAB ingest (app/rag/ingest.py): Docling converts the PDF, Granite (via watsonx,
# already a runtime dep through httpx) embeds the chunks. Large; pulls model weights.
docling>=2.0
