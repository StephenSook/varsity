# Runtime + test deps (CI installs these). Heavy build-time data libs are in requirements-data.txt.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
sse-starlette>=2.1
httpx>=0.27
python-dotenv>=1.0
mcp>=1.12
a2a-sdk>=1.0.3
# FAISS-backed Law retrieval (the online Granite-embedding path queries the
# index built at ingest; the offline/CI keyword path does not import these).
faiss-cpu>=1.9
numpy>=1.26
# OpenTelemetry: per-request span tree (HTTP + pipeline stages) to a console exporter.
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
pytest>=8.3
